import React from 'react';
import { Icon as ChakraIcon, IconProps } from '@chakra-ui/react';

// Re-export Icon to ensure it's always available
export const Icon = ChakraIcon;

// Default export for convenience
export default Icon;
