# DigiSol.AI Production Environment Variables
# Copy this file and fill in your actual values
# Then add these to your Render.com environment variables

# =============================================================================
# 🔐 CORE DJANGO SETTINGS (REQUIRED)
# =============================================================================

# Generate a secure secret key: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-generated-secret-key-here

# Your domain names (comma-separated)
ALLOWED_HOSTS=digisol-backend.onrender.com,www.digisolai.ca,digisolai.ca

# CSRF trusted origins for your domains
CSRF_TRUSTED_ORIGINS=https://*.digisolai.ca,https://*.netlify.app,https://*.onrender.com

# =============================================================================
# 🗄️ DATABASE CONFIGURATION (REQUIRED)
# =============================================================================

# Option 1: AWS RDS PostgreSQL (Recommended)
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_database_password
DB_HOST=your-rds-endpoint.region.rds.amazonaws.com
DB_PORT=5432

# Option 2: Alternative DATABASE_URL (if not using AWS RDS)
# DATABASE_URL=postgresql://user:password@host:port/dbname

# =============================================================================
# 💳 STRIPE PAYMENT PROCESSING (REQUIRED for billing)
# =============================================================================

# Get these from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_live_your-stripe-secret-key-here
STRIPE_PUBLISHABLE_KEY=pk_live_your-stripe-publishable-key-here

# Get this from: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_your-stripe-webhook-secret-here

# =============================================================================
# 🤖 GOOGLE GEMINI AI (REQUIRED for AI features)
# =============================================================================

# Get this from: https://makersuite.google.com/app/apikey
GOOGLE_GEMINI_API_KEY=your-gemini-api-key-here
GOOGLE_APPLICATION_CREDENTIALS_JSON={}

# =============================================================================
# ☁️ AWS S3 STORAGE (Optional but Recommended)
# =============================================================================

# Get these from AWS IAM Console
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_STORAGE_BUCKET_NAME=your-s3-bucket-name
AWS_S3_REGION_NAME=us-east-1

# =============================================================================
# 📧 EMAIL CONFIGURATION (Optional)
# =============================================================================

DEFAULT_FROM_EMAIL=noreply@digisolai.ca
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# =============================================================================
# 🔄 REDIS/CELERY (Optional for background tasks)
# =============================================================================

# Get this from your Redis provider (Upstash, Redis Cloud, etc.)
REDIS_URL=redis://your-redis-url:port

# =============================================================================
# 📊 ANALYTICS (Optional)
# =============================================================================

# Get this from Google Analytics
GOOGLE_ANALYTICS_ID=your-ga-id-here

# =============================================================================
# 🎯 QUICK SETUP CHECKLIST
# =============================================================================

# ✅ Generate Django Secret Key
# ✅ Set up AWS RDS PostgreSQL database
# ✅ Get Stripe API keys (live keys for production)
# ✅ Get Google Gemini API key
# ✅ Set up AWS S3 bucket (optional)
# ✅ Configure Redis (optional)
# ✅ Set up Google Analytics (optional)

# =============================================================================
# 🔒 SECURITY REMINDERS
# =============================================================================

# - Never commit these values to Git
# - Use different keys for development and production
# - Rotate your API keys regularly
# - Keep your database credentials secure
# - Use HTTPS in production

# =============================================================================
# 📋 DEPLOYMENT STEPS
# =============================================================================

# 1. Copy this file and fill in your actual values
# 2. Add each variable to your Render.com environment variables
# 3. Test your setup using the test scripts
# 4. Deploy to production
# 5. Verify all features work correctly

# =============================================================================
# 🧪 TESTING COMMANDS
# =============================================================================

# Test database connection:
# cd backend && python check_rds_connection.py

# Test Stripe integration:
# cd backend && python manage.py shell
# >>> from django.conf import settings
# >>> import stripe
# >>> stripe.api_key = settings.STRIPE_SECRET_KEY
# >>> stripe.Customer.list(limit=1)

# Test Gemini AI:
# cd backend && python manage.py shell
# >>> from django.conf import settings
# >>> print(f"Gemini configured: {bool(settings.GOOGLE_GEMINI_API_KEY)}")

# Test S3 storage:
# cd backend && python manage.py shell
# >>> from django.conf import settings
# >>> print(f"S3 configured: {bool(settings.AWS_ACCESS_KEY_ID)}")
